---
- name:
  hosts: all
  vars:
  connection: httpapi
  gather_facts: false
  tasks:
    - name: Create
      fwebos_virtual_server:
       action: add
       name: test4
       vdom: root

    - name: delete
      fwebos_virtual_server:
       action: delete
       name: test4
       vdom: root

